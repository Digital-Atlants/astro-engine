"""Rectification benchmark harness.

Measurement instrument only. Nothing in this package is imported by the
service; `benchmarks/` is not copied into the Docker image and adds no
runtime dependency to `astro_engine`.
"""
